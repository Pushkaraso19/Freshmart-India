{
	"name": "freshmart-backend",
	"version": "1.0.0",
	"description": "FreshMart backend API (Node + Express + PostgreSQL)",
	"main": "src/server.js",
	"license": "MIT",
	"scripts": {
			"dev": "nodemon src/server.js",
			"start": "node src/server.js",
			"migrate": "node src/db/ensureDb.js && node src/db/migrate.js",
		"migrate:drop": "node src/db/migrate.js --drop",
			"seed": "node src/db/ensureDb.js && node src/db/seed.js"
	},
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"cors": "^2.8.5",
		"dotenv": "^16.4.5",
		"express": "^4.19.2",
		"jsonwebtoken": "^9.0.2",
		"morgan": "^1.10.0",
		"pg": "^8.12.0"
	},
	"devDependencies": {
		"nodemon": "^3.1.0"
	}
}
